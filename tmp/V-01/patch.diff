diff --git a/package.json b/package.json
index cc55264..66d97f4 100644
--- a/package.json
+++ b/package.json
@@ -6,7 +6,8 @@
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
-    "export": "next build"
+    "export": "next build",
+    "screenshot:proof": "node scripts/proof-screenshot-pack.mjs"
   },
   "dependencies": {
     "framer-motion": "^11.0.24",
@@ -17,6 +18,7 @@
   "devDependencies": {
     "autoprefixer": "^10.4.19",
     "postcss": "^8.4.39",
-    "tailwindcss": "^3.4.4"
+    "tailwindcss": "^3.4.4",
+    "playwright": "^1.54.2"
   }
 }
diff --git a/scripts/proof-screenshot-pack.mjs b/scripts/proof-screenshot-pack.mjs
new file mode 100644
index 0000000..e540e6f
--- /dev/null
+++ b/scripts/proof-screenshot-pack.mjs
@@ -0,0 +1,73 @@
+import fs from 'node:fs/promises';
+import path from 'node:path';
+import { chromium, devices } from 'playwright';
+
+const outputDir = path.resolve(process.cwd(), 'tmp/V-01/screenshots');
+
+const targets = [
+  { slug: 'home', url: 'https://hamburgwebsites.de/' },
+  { slug: 'preise', url: 'https://hamburgwebsites.de/preise' },
+  { slug: 'kontakt', url: 'https://hamburgwebsites.de/kontakt' },
+  {
+    slug: 'autoservice-demo',
+    url: 'https://hamburgwebsites.de/referenzen/autoservice-demo'
+  }
+];
+
+const desktopViewport = { width: 1440, height: 900 };
+const mobile = devices['iPhone 12'];
+
+const makeFileName = (slug, variant) => `${slug}-${variant}.png`;
+
+const captureDesktop = async (browser, target) => {
+  const context = await browser.newContext({
+    viewport: desktopViewport,
+    deviceScaleFactor: 1,
+    isMobile: false,
+    hasTouch: false
+  });
+
+  const page = await context.newPage();
+  await page.goto(target.url, { waitUntil: 'networkidle', timeout: 60000 });
+  await page.screenshot({
+    path: path.join(outputDir, makeFileName(target.slug, 'desktop')),
+    fullPage: true
+  });
+  await context.close();
+};
+
+const captureMobile = async (browser, target) => {
+  const context = await browser.newContext({
+    ...mobile,
+    viewport: { width: 390, height: 844 }
+  });
+
+  const page = await context.newPage();
+  await page.goto(target.url, { waitUntil: 'networkidle', timeout: 60000 });
+  await page.screenshot({
+    path: path.join(outputDir, makeFileName(target.slug, 'mobile')),
+    fullPage: true
+  });
+  await context.close();
+};
+
+const run = async () => {
+  await fs.mkdir(outputDir, { recursive: true });
+
+  const browser = await chromium.launch({ headless: true });
+
+  try {
+    for (const target of targets) {
+      await captureDesktop(browser, target);
+      await captureMobile(browser, target);
+      process.stdout.write(`Captured: ${target.slug}\n`);
+    }
+  } finally {
+    await browser.close();
+  }
+};
+
+run().catch((error) => {
+  process.stderr.write(`${error.stack || error}\n`);
+  process.exitCode = 1;
+});
